import React, {Component, PropTypes} from 'react';

const PackingListContainerTitle = (props) => {
  
  return (
    <div>
      <h2 id="packing-list-header">{props.title}</h2>
    </div>
  );
}

export default PackingListContainerTitle;